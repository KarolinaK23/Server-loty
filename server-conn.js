/*var mongoose = require('mongoose');

var HOST_NAME = 'localhost';
var DATABASE_NAME = 'flights';

var con = mongoose.createConnection('mongodb://' + HOST_NAME + '/' + DATABASE_NAME);

function connection(){
    return con;
}

module.exports = connection; */